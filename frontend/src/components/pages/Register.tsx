import AuthCard from "../auth/AuthCard";
import RegisterForm from "../auth/RegisterForm";

function Register() {
  return (
    <AuthCard>
      <RegisterForm />
    </AuthCard>
  );
}

export default Register;
